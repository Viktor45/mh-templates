# ============================================================================
# ШАБЛОН STARGATE
# Полная конфигурация Mihomo/Meta с расширенными возможностями маршрутизации
# Включает поддержку ZKEEN геоданных, GEOIP/GEOSITE правил и CDN-провайдеров
# Документация: https://github.com/MetaCubeX/mihomo/blob/Meta/docs/config.yaml
# ============================================================================

# ============================================================================
# === ОСНОВНЫЕ ПАРАМЕТРЫ ===
# ============================================================================

# Разрешить подключения из локальной сети (например, с других устройств дома)
# Если true — другие устройства в вашей сети смогут использовать этот прокси
# Если false — только локальное устройство имеет доступ к прокси-серверу
allow-lan: true

# Режим работы ядра: определяет логику маршрутизации всего трафика
# "rule" — трафик распределяется по правилам (рекомендуется для гибкости)
# "global" — весь трафик идёт через выбранный прокси-сервер
# "direct" — весь трафик идёт напрямую без использования прокси
mode: rule

# Уровень подробности логирования событий в журнале работы
# Доступные значения: silent (тихо), error (ошибки), warning (предупреждения),
# info (информация), debug (отладка - максимальная детализация)
log-level: $LOG_LEVEL

# Включить поддержку IPv6
ipv6: $IPV6

# Делать задержки более естественными (чтобы не выдавать автоматизацию)
# Улучшает точность тестирования скорости прокси-серверов
# Рекомендуется держать включённым для стабильной работы
unified-delay: true

# Адрес и порт для управления через веб-интерфейс (например: 0.0.0.0:9090)
# 0.0.0.0 — слушать все сетевые интерфейсы (доступ из сети)
external-controller: $EXTERNAL_CONTROLLER_ADDRESS:$UI_PORT

# Путь к папке с веб-интерфейсом на диске (локальная версия UI)
# Если папка существует, интерфейс загрузится оттуда без скачивания
external-ui: $EXTERNAL_UI_PATH

# Ссылка на архив с интерфейсом в интернете (скачается автоматически при старте)
# Используется, если локальная папка пуста или отсутствует
external-ui-url: $EXTERNAL_UI_URL

# Секретный ключ для защиты доступа к веб-панели (API Secret)
# Рекомендуется использовать сложную строку для безопасности
# Без этого ключа любой в локальной сети сможет управлять прокси
secret: $UI_SECRET

# ============================================================================
# === ГЕОДАННЫЕ (ZKEEN) ===
# ============================================================================

# Включить использование улучшенных списков стран и сайтов от ZKEEN
# ZKEEN предоставляет более точные и актуальные геоданные для маршрутизации
geodata-mode: true

# Ссылки на файлы с правилами по странам (IP) и сайтам (домены)
# Эти файлы загружаются автоматически и кэшируются локально
geox-url:
  geosite: "https://github.com/jameszeroX/zkeen-domains/releases/latest/download/zkeen.dat"
  geoip: "https://github.com/jameszeroX/zkeen-ip/releases/latest/download/zkeenip.dat"

# Автоматически обновлять эти файлы раз в сутки
geo-auto-update: true

# Интервал обновления в часах (24 часа = один раз в сутки)
geo-update-interval: 24

# ============================================================================
# === АНАЛИЗ ТРАФИКА (SNIFFER) ===
# ============================================================================

# Функция "подслушивания" трафика для определения доменных имён
# Позволяет корректно маршрутизировать трафик даже при подключении по IP-адресу
# Критически важно для работы правил на основе доменных имён
sniffer:
  enable: true    # Включить сниффер
  parse-pure-ip: true    # Анализировать IP-адреса без домена
  force-dns-mapping: true    # Принудительно использовать DNS-маппинг
  sniff:    # Настройки для различных протоколов
    HTTP:
      ports: [80, 8080-8880]    # Порты для HTTP-трафика
      override-destination: true    # Подменять получателя трафика на домен
    TLS:
      ports: [443, 8443]    # Порты для HTTPS/TLS-трафика
    QUIC:
      ports: [443, 8443]    # Порты для QUIC-протокола (UDP/443)

# ============================================================================
# === DNS НАСТРОЙКИ ===
# ============================================================================

# Конфигурация DNS-резолвера для преобразования доменов в IP-адреса
# Отвечает за разрешение доменных имён в сетевые адреса
dns:
  enable: $DNS_ENABLE    # Включить собственный DNS-резолвер
  use-system-hosts: $DNS_USE_SYSTEM_HOSTS    # Использовать системный файл hosts
  nameserver:
    - system    # Использовать системный DNS (от роутера/провайдера)

# ============================================================================
# === СЛУШАТЕЛИ (LISTENERS) ===
# ============================================================================

# Точки входа для принятия трафика от приложений и системы
# Mihomo поддерживает множественные слушатели разных типов
# Каждый слушатель работает независимо и может иметь свои настройки
listeners:
  - name: mixed-in    # Смешанный прокси (HTTP + SOCKS5)
    type: mixed    # Тип: mixed (принимает оба протокола)
    port: $MIXED_PORT    # Порт для приложений (обычно 1080)
  - name: tun-in    # TUN-интерфейс (системный уровень)
    type: tun    # Тип: tun (виртуальный сетевой интерфейс)
    stack: $TUN_STACK    # Стек: system, gvisor, mixed
    auto-detect-interface: $TUN_AUTO_DETECT_INTERFACE    # Автоопределение сетевого интерфейса
    auto-route: $TUN_AUTO_ROUTE    # Автоматическая настройка маршрутов
    auto-redirect: $TUN_AUTO_REDIRECT    # Перехват трафика без настройки прокси в приложениях
    inet4-address:
      - $TUN_INET4_ADDRESS    # IP-адрес виртуального TUN-интерфейса

# ============================================================================
# === ПОДПИСКИ НА ПРОКСИ (PROXY PROVIDERS) ===
# ============================================================================

# Источники прокси-серверов (подписки)
# PROVIDERS_BLOCK — автоматически подставляемые подписки из скрипта настройки
# Формат подписок должен быть совместим с Mihomo/Meta
proxy-providers:
$PROVIDERS_BLOCK

# ============================================================================
# === ОБЩИЕ ПАРАМЕТРЫ ПРОВЕРКИ (ANCHORS) ===
# ============================================================================

# Базовая конфигурация проверки здоровья прокси (health check)
# Используется для fallback-групп (автоматическое переключение при отказе)
# Проверяет доступность прокси-сервера по указанному URL
health_check: &health_check
  url: $HEALTH_CHECK_URL    # URL для проверки доступности прокси
  interval: 200    # Интервал проверки в секундах
  expected-status: $HEALTH_CHECK_EXPECTED_STATUS    # Ожидаемый HTTP-статус ответа

# Конфигурация URL-тестирования для выбора fastest-прокси
# Используется для url-test-групп (автоматический выбор по скорости)
# Измеряет задержку до указанного URL для каждого прокси
url_test: &url_test
  url: $HEALTH_CHECK_URL    # URL для измерения задержки
  interval: 300    # Интервал тестирования в секундах
  lazy: false    # Тестировать сразу при запуске
  expected-status: $HEALTH_CHECK_EXPECTED_STATUS    # Ожидаемый HTTP-статус
  tolerance: 50    # Допустимая разница задержки (мс)

# Специальная конфигурация для AWG-прокси с уменьшенной толерантностью
url_test_awg: &url_test_awg
  <<: *url_test    # Наследовать параметры url_test
  tolerance: 30    # Более строгий выбор (30 мс)

# ============================================================================
# === СПИСКИ ПРОВАЙДЕРОВ И ФИЛЬТРЫ (ANCHORS) ===
# ============================================================================

# Список провайдеров для основных прокси
# Используется в группах типа url-test и fallback
providers_list: &providers_list
$PROVIDERS_LIST

# Регулярное выражение для исключения российских прокси
# Используется в группах для иностранного трафика
exclude_ru: &exclude_ru "(?i)🇷🇺|RU"

# Регулярное выражение для фильтрации только российских прокси
# Используется в группах для российского трафика
filter_ru: &filter_ru "(?i)🇷🇺|RU"

# Регулярное выражение для исключения не-AWG протоколов
# Оставляет только WireGuard/AmneziaWG прокси
exclude_awg: &exclude_awg "ss|shadowsocks|trojan|hysteria|hysteria2|tuic|socks5|http|vless|vmess"

# ============================================================================
# === СПИСКИ ПРОКСИ-ГРУПП (ANCHORS) ===
# ============================================================================

# Полный список всех прокси-групп для основного селектора
# Определяет порядок отображения групп в веб-интерфейсе
proxy_list_all: &proxy_list_all
  - SELECTOR
  - AUTO
  - MANUAL
  - AWG_AUTO
  - AWG_MANUAL
  - RU_AUTO
  - RU_MANUAL
  - DIRECT

# Специальный список для YouTube (с приоритетом российских прокси)
# Оптимизирован для работы с YouTube через российские серверы
proxy_list_youtube: &proxy_list_youtube
  - RU_AUTO
  - RU_MANUAL
  - SELECTOR
  - AWG_AUTO
  - AWG_MANUAL
  - AUTO
  - MANUAL
  - DIRECT

# ============================================================================
# === ИКОНКИ (ОТДЕЛЬНЫЕ ЯКОРЯ ДЛЯ КАЖДОЙ) ===
# ============================================================================

icon_telegram: &icon_telegram https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Telegram.png
icon_whatsapp: &icon_whatsapp https://github.com/OMchik33/Whatsapp-icon/blob/master/icons/whatsapp_194x194.png?raw=true
icon_russia: &icon_russia https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Russia.png
icon_direct: &icon_direct https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Direct.png
icon_facebook: &icon_facebook https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Facebook.png
icon_amazon: &icon_amazon https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Amazon.png
icon_global: &icon_global https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Global.png
icon_google: &icon_google https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Google.png
icon_cloudflare: &icon_cloudflare https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Cloudflare.png
icon_steam: &icon_steam https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Steam.png
icon_discord: &icon_discord https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Discord.png
icon_youtube: &icon_youtube https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/YouTube.png
icon_twitch: &icon_twitch https://img.icons8.com/3d-fluency/94/twitch.png
icon_bot: &icon_bot https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Dark/Bot.png
icon_microsoft: &icon_microsoft https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Dark/Microsoft.png
icon_download: &icon_download https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Download.png
icon_media: &icon_media https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Media.png
icon_puzzle: &icon_puzzle https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Puzzle.png

# ============================================================================
# === БАЗОВЫЕ ШАБЛОНЫ ПРАВИЛ (RULE PROVIDER BASES) ===
# ============================================================================

# Базовый шаблон для всех HTTP-провайдеров правил
# Сокращает повторение type: http и interval: 86400
rule_provider_base: &rule_provider_base
  type: http    # Тип: HTTP (скачивать из интернета)
  interval: 86400    # Интервал обновления: 24 часа

# Комбинации behavior + format для разных типов правил
# Сокращает повторение этих двух параметров в каждом правиле
rule_behavior_domain_mrs: &rule_behavior_domain_mrs
  <<: *rule_provider_base    # Наследовать базовые параметры
  behavior: domain    # Тип правила: домены
  format: mrs    # Формат: Meta Ruleset

rule_behavior_domain_yaml: &rule_behavior_domain_yaml
  <<: *rule_provider_base
  behavior: domain    # Тип правила: домены
  format: yaml    # Формат: YAML

rule_behavior_ipcidr_mrs: &rule_behavior_ipcidr_mrs
  <<: *rule_provider_base
  behavior: ipcidr    # Тип правила: IP-диапазоны
  format: mrs    # Формат: Meta Ruleset

rule_behavior_ipcidr_yaml: &rule_behavior_ipcidr_yaml
  <<: *rule_provider_base
  behavior: ipcidr    # Тип правила: IP-диапазоны
  format: yaml    # Формат: YAML

# Специальный шаблон для классических правил (torrent-clients)
rule_behavior_classical_yaml: &rule_behavior_classical_yaml
  <<: *rule_provider_base
  behavior: classical    # Тип правила: классический
  format: yaml    # Формат: YAML

# ============================================================================
# === ГРУППЫ ПРОКСИ (PROXY GROUPS) ===
# ============================================================================

# Группы определяют логику выбора прокси для различных сценариев
# Типы групп: select (ручной), url-test (автоматический по скорости), fallback (резервный)
proxy-groups:
    # --------------------------------------------------------------------------
    # ГЛАВНЫЙ СЕЛЕКТОР — основная точка выбора в веб-интерфейсе
    # --------------------------------------------------------------------------
  - name: SELECTOR
    type: select    # Ручной выбор пользователем
    include-all: true    # Включить все доступные прокси
    proxies:
      - AUTO
      - MANUAL
      - AWG_AUTO
      - AWG_MANUAL
      - RU_AUTO
      - RU_MANUAL
      - DIRECT

    # --------------------------------------------------------------------------
    # БАЗОВЫЕ АВТОМАТИЧЕСКИЕ ГРУППЫ (ИНОСТРАННЫЕ ПРОКСИ)
    # --------------------------------------------------------------------------

    # AUTO — полностью автоматический выбор (иностранные)
    # Приоритет: FASTEST → FAILOVER → MANUAL → DIRECT
  - name: AUTO
    type: fallback    # Переключение при отказе
    proxies:
      - FASTEST
      - FAILOVER
      - MANUAL
      - DIRECT
    <<: *health_check    # Параметры проверки

    # FASTEST — автоматически выбирает самый быстрый прокси по задержке
  - name: FASTEST
    type: url-test    # Тестирование скорости
    use: *providers_list    # Использовать провайдеры
    exclude-filter: *exclude_ru    # Исключить российские серверы
    <<: *url_test    # Параметры тестирования

    # FAILOVER — резервный прокси при отказе основного
  - name: FAILOVER
    type: fallback    # Переключение при отказе
    use: *providers_list    # Использовать провайдеры
    exclude-filter: *exclude_ru    # Исключить российские серверы
    <<: *health_check    # Параметры проверки

    # MANUAL — ручной выбор иностранного прокси
  - name: MANUAL
    type: select    # Ручной выбор
    use: *providers_list    # Использовать провайдеры
    exclude-filter: *exclude_ru    # Исключить российские серверы

    # --------------------------------------------------------------------------
    # РОССИЙСКИЕ ПРОКСИ-ГРУППЫ
    # --------------------------------------------------------------------------

    # RU_AUTO — полностью автоматический выбор (российские)
    # Приоритет: RU_FASTEST → RU_FAILOVER → RU_MANUAL → DIRECT
  - name: RU_AUTO
    type: fallback    # Переключение при отказе
    proxies:
      - RU_FASTEST
      - RU_FAILOVER
      - RU_MANUAL
      - DIRECT
    filter: *filter_ru    # Только российские серверы
    <<: *health_check    # Параметры проверки

    # RU_MANUAL — ручной выбор российского прокси
  - name: RU_MANUAL
    type: select    # Ручной выбор
    use: *providers_list    # Использовать провайдеры
    filter: *filter_ru    # Только российские серверы

    # RU_FASTEST — самый быстрый российский прокси
  - name: RU_FASTEST
    type: url-test    # Тестирование скорости
    use: *providers_list    # Использовать провайдеры
    filter: *filter_ru    # Только российские серверы
    <<: *url_test    # Параметры тестирования

    # RU_FAILOVER — резервный российский прокси
  - name: RU_FAILOVER
    type: fallback    # Переключение при отказе
    use: *providers_list    # Использовать провайдеры
    filter: *filter_ru    # Только российские серверы
    <<: *health_check    # Параметры проверки

    # --------------------------------------------------------------------------
    # AMNEZIA/WIREGUARD ГРУППЫ
    # --------------------------------------------------------------------------

    # AWG_AUTO — автоматический выбор AWG-прокси
    # Приоритет: AWG_FASTEST → AWG_FAILOVER → AWG_MANUAL → DIRECT
  - name: AWG_AUTO
    type: fallback    # Переключение при отказе
    proxies:
      - AWG_FASTEST
      - AWG_FAILOVER
      - AWG_MANUAL
      - DIRECT
    exclude-type: *exclude_awg    # Только AWG-протоколы
    <<: *health_check    # Параметры проверки

    # AWG_MANUAL — ручной выбор AWG-прокси
  - name: AWG_MANUAL
    type: select    # Ручной выбор
    use: *providers_list    # Использовать провайдеры
    exclude-type: *exclude_awg    # Только AWG-протоколы

    # AWG_FASTEST — самый быстрый AWG-прокси
  - name: AWG_FASTEST
    type: url-test    # Тестирование скорости
    use: *providers_list    # Использовать провайдеры
    exclude-type: *exclude_awg    # Только AWG-протоколы
    <<: *url_test_awg    # Параметры тестирования (tolerance: 30)

    # AWG_FAILOVER — резервный AWG-прокси
  - name: AWG_FAILOVER
    type: fallback    # Переключение при отказе
    use: *providers_list    # Использовать провайдеры
    exclude-type: *exclude_awg    # Только AWG-протоколы
    <<: *health_check    # Параметры проверки

    # --------------------------------------------------------------------------
    # СИСТЕМНЫЕ ГРУППЫ
    # --------------------------------------------------------------------------

    # GLOBAL — скрытая глобальная группа для правил
  - name: GLOBAL
    type: select    # Ручной выбор
    hidden: true    # Скрыть из интерфейса
    icon: *icon_global    # Иконка группы
    proxies: *proxy_list_all    # Список доступных опций

    # QUIC — обработка QUIC-трафика (UDP/443)
  - name: QUIC
    type: select    # Ручной выбор
    proxies:
      - PASS    # Пропустить без изменений
      - REJECT    # Заблокировать

    # --------------------------------------------------------------------------
    # ГРУППЫ ПО ПРИЛОЖЕНИЯМ/СЕРВИСАМ
    # --------------------------------------------------------------------------

    # TELEGRAM — мессенджер Telegram
  - name: Telegram
    type: select    # Ручной выбор
    icon: *icon_telegram    # Иконка Telegram
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # WHATSAPP — мессенджер WhatsApp
  - name: WhatsApp
    type: select    # Ручной выбор
    icon: *icon_whatsapp    # Иконка WhatsApp
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # RU-TRAFFIC — российский трафик (по GEOIP)
  - name: Ru-Traffic
    type: select    # Ручной выбор
    icon: *icon_russia    # Иконка России
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # ECH-REFILTER — трафик для ускорения интернета с ECH
  - name: ECH-Refilter
    type: select    # Ручной выбор
    icon: *icon_direct    # Иконка Direct
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # META — сервисы Meta (Facebook, Instagram, WhatsApp)
  - name: META
    type: select    # Ручной выбор
    icon: *icon_facebook    # Иконка Facebook
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # AMAZON — сервисы Amazon (AWS, магазин, Prime)
  - name: AMAZON
    type: select    # Ручной выбор
    icon: *icon_amazon    # Иконка Amazon
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # FASTLY — CDN-провайдер Fastly
  - name: FASTLY
    type: select    # Ручной выбор
    icon: *icon_global    # Иконка Global
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # AKAMAI — CDN-провайдер Akamai
  - name: AKAMAI
    type: select    # Ручной выбор
    icon: *icon_global    # Иконка Global
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # GOOGLE — сервисы Google (поиск, YouTube, Gmail)
  - name: GOOGLE
    type: select    # Ручной выбор
    icon: *icon_google    # Иконка Google
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # CLOUDFLARE — сервисы Cloudflare (CDN, DNS, защита)
  - name: CLOUDFLARE
    type: select    # Ручной выбор
    icon: *icon_cloudflare    # Иконка Cloudflare
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # CDN77 — CDN-провайдер CDN77
  - name: CDN77
    type: select    # Ручной выбор
    icon: *icon_global    # Иконка Global
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # DIGITALOCEAN — облачный провайдер DigitalOcean
  - name: DIGITALOCEAN
    type: select    # Ручной выбор
    icon: *icon_global    # Иконка Global
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # HETZNER — облачный провайдер Hetzner
  - name: HETZNER
    type: select    # Ручной выбор
    icon: *icon_global    # Иконка Global
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # MEGA — облачное хранилище MEGA
  - name: MEGA
    type: select    # Ручной выбор
    icon: *icon_global    # Иконка Global
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # ORACLE — облачный провайдер Oracle Cloud
  - name: ORACLE
    type: select    # Ручной выбор
    icon: *icon_global    # Иконка Global
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # OVH — облачный провайдер OVH
  - name: OVH
    type: select    # Ручной выбор
    icon: *icon_global    # Иконка Global
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # VULTR — облачный провайдер Vultr
  - name: VULTR
    type: select    # Ручной выбор
    icon: *icon_global    # Иконка Global
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # STEAM — игровая платформа Steam
  - name: Steam
    type: select    # Ручной выбор
    icon: *icon_steam    # Иконка Steam
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # DISCORD — голосовой/текстовый чат Discord
  - name: Discord
    type: select    # Ручной выбор
    icon: *icon_discord    # Иконка Discord
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # YOUTUBE — видеохостинг YouTube
  - name: YouTube
    type: select    # Ручной выбор
    icon: *icon_youtube    # Иконка YouTube
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_youtube    # Специальный список для YouTube

    # TWITCH — стриминговая платформа Twitch
  - name: Twitch
    type: select    # Ручной выбор
    icon: *icon_twitch    # Иконка Twitch
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # AI & РАЗРАБОТКА — искусственный интеллект и инструменты разработки
  - name: AI & Разработка
    type: select    # Ручной выбор
    icon: *icon_bot    # Иконка бота/ИИ
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # GOOGLE_GEMINI — ИИ-модель Google Gemini
  - name: GoogleGemini
    type: select    # Ручной выбор
    icon: *icon_bot    # Иконка бота/ИИ
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # OPENAI — ИИ-модели OpenAI (ChatGPT, API)
  - name: OpenAI
    type: select    # Ручной выбор
    icon: *icon_bot    # Иконка бота/ИИ
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # MICROSOFT — сервисы Microsoft (Azure, Office, Copilot)
  - name: Microsoft
    type: select    # Ручной выбор
    icon: *icon_microsoft    # Иконка Microsoft
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # ANTHROPIC — ИИ-модели Anthropic (Claude)
  - name: Anthropic
    type: select    # Ручной выбор
    icon: *icon_bot    # Иконка бота/ИИ
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # PERPLEXITY — поисковая система с ИИ Perplexity
  - name: Perplexity
    type: select    # Ручной выбор
    icon: *icon_bot    # Иконка бота/ИИ
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # TORRENT-TRAFFIC — торрент-трафик (клиенты, трекеры, сайты)
  - name: Torrent-Traffic
    type: select    # Ручной выбор
    icon: *icon_download    # Иконка загрузки
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # POLITIC-DOMAINS — политические и новостные домены
  - name: Politic-Domains
    type: select    # Ручной выбор
    icon: *icon_media    # Иконка медиа
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

    # OTHER-DOMAINS — остальные домены (по GEOSITE)
  - name: Other-Domains
    type: select    # Ручной выбор
    icon: *icon_puzzle    # Иконка пазла
    include-all: false    # Только указанные прокси
    proxies: *proxy_list_all    # Список доступных опций

# ============================================================================
# === ВНЕШНИЕ НАБОРЫ ПРАВИЛ (RULE PROVIDERS) ===
# ============================================================================

# Провайдеры правил — загружают списки доменов и IP для маршрутизации
# Позволяют автоматически обновлять правила без изменения конфигурации
rule-providers:
    # Telegram — домены и IP-адреса
  telegram-domains:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/MetaCubeX/meta-rules-dat/raw/refs/heads/meta/geo/geosite/telegram.mrs
    path: ./rule-sets/telegram-domains.mrs

  telegram-ips:
    <<: *rule_behavior_ipcidr_yaml
    url: https://raw.githubusercontent.com/OMchik33/custom-rules/refs/heads/main/mihomo/telegram-ips.yaml
    path: ./rule-sets/telegram-ips.yaml

    # WhatsApp — домены и IP-адреса
  whatsapp-domains:
    <<: *rule_behavior_domain_mrs
    url: https://raw.githubusercontent.com/MetaCubeX/meta-rules-dat/meta/geo/geosite/whatsapp.mrs
    path: ./rule-sets/whatsapp-domains.mrs

  whatsapp-ips:
    <<: *rule_behavior_ipcidr_yaml
    url: https://raw.githubusercontent.com/OMchik33/custom-rules/refs/heads/main/mihomo/whatsapp-ips.yaml
    path: ./rule-sets/whatsapp-ips.yaml

    # Steam — игровая платформа (домены и IP)
  steam-domains:
    <<: *rule_behavior_domain_yaml
    url: https://raw.githubusercontent.com/OMchik33/custom-rules/refs/heads/main/mihomo/gaming-domains.yaml
    path: ./rule-sets/steam-domains.yaml

  steam-ips:
    <<: *rule_behavior_ipcidr_yaml
    url: https://raw.githubusercontent.com/OMchik33/custom-rules/refs/heads/main/mihomo/gaming-ips.yaml
    path: ./rule-sets/steam-ips.yaml

    # Twitch — стриминговая платформа
  twitch-domains:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/MetaCubeX/meta-rules-dat/raw/refs/heads/meta/geo/geosite/twitch.mrs
    path: ./rule-sets/twitch-domains.mrs

    #  ru-app-list:
    #  <<: *rule_behavior_classical_yaml
    #    url: 'https://github.com/legiz-ru/mihomo-rule-sets/raw/main/other/ru-app-list.yaml'
    #    path: ./rule-sets/ru-app-list.yaml

    # Discord — чат и голосовые серверы
  discord-domains:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/MetaCubeX/meta-rules-dat/raw/meta/geo/geosite/discord.mrs
    path: ./rule-sets/discord-domains.mrs

  discord-voiceips:
    <<: *rule_behavior_ipcidr_mrs
    url: https://github.com/legiz-ru/mihomo-rule-sets/raw/main/other/discord-voice-ip-list.mrs
    path: ./rule-sets/discord-voiceips.mrs

    # Google Gemini — ИИ-сервис Google
  google-gemini:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/MetaCubeX/meta-rules-dat/raw/meta/geo/geosite/google-gemini.mrs
    path: ./rule-sets/google-gemini.mrs

    # OpenAI — ИИ-сервисы (ChatGPT, API)
  openai:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/MetaCubeX/meta-rules-dat/raw/meta/geo/geosite/openai.mrs
    path: ./rule-sets/openai.mrs

    # Microsoft — сервисы Microsoft
  microsoft:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/MetaCubeX/meta-rules-dat/raw/meta/geo/geosite/microsoft.mrs
    path: ./rule-sets/microsoft.mrs

    # Anthropic — ИИ-сервисы (Claude)
  anthropic:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/MetaCubeX/meta-rules-dat/raw/meta/geo/geosite/anthropic.mrs
    path: ./rule-sets/anthropic.mrs

    # Perplexity — ИИ-поисковик
  perplexity:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/MetaCubeX/meta-rules-dat/raw/meta/geo/geosite/perplexity.mrs
    path: ./rule-sets/perplexity.mrs

    # YouTube — видеохостинг
  youtube:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/MetaCubeX/meta-rules-dat/raw/meta/geo/geosite/youtube.mrs
    path: ./rule-sets/youtube.mrs

    # Re-filter — правила для ускорения интернета с ECH
  refilter-noech:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/legiz-ru/mihomo-rule-sets/raw/main/re-filter/re-filter-noech.mrs
    path: ./rule-sets/refilter-noech.mrs

  refilter-ech:
    <<: *rule_behavior_domain_mrs
    url: https://github.com/legiz-ru/mihomo-rule-sets/raw/main/re-filter/re-filter-ech.mrs
    path: ./rule-sets/refilter-ech.mrs

  refilter-ipsum:
    <<: *rule_behavior_ipcidr_mrs
    url: https://github.com/legiz-ru/mihomo-rule-sets/raw/main/re-filter/ip-rule.mrs
    path: ./rule-sets/refilter-ip-rule.mrs

    # Torrent — торрент-клиенты, трекеры и сайты
  torrent-clients:
    <<: *rule_behavior_classical_yaml
    url: https://github.com/legiz-ru/mihomo-rule-sets/raw/main/other/torrent-clients.yaml
    path: ./rule-sets/torrent-clients.yaml

  torrent-trackers:
    <<: *rule_behavior_domain_mrs
    url: https://raw.githubusercontent.com/legiz-ru/mihomo-rule-sets/main/other/torrent-trackers.mrs
    path: ./rule-sets/torrent-trackers.mrs

  torrent-websites:
    <<: *rule_behavior_domain_mrs
    url: https://raw.githubusercontent.com/legiz-ru/mihomo-rule-sets/main/other/torrent-websites.mrs
    path: ./rule-sets/torrent-websites.mrs

    # Правила блокировки рекламы и нежелательного контента (OISD)
    #  oisd_big:
    #    <<: *rule_behavior_domain_mrs
    #    url: "https://github.com/legiz-ru/mihomo-rule-sets/raw/main/oisd/big.mrs"
    #    path: ./oisd/big.mrs

    #  oisd_small:
    #    <<: *rule_behavior_domain_mrs
    #    url: "https://github.com/legiz-ru/mihomo-rule-sets/raw/main/oisd/small.mrs"
    #    path: ./oisd/small.mrs

    #  oisd_nsfw_small:
    #    <<: *rule_behavior_domain_mrs
    #    url: "https://github.com/legiz-ru/mihomo-rule-sets/raw/main/oisd/nsfw_small.mrs"
    #    path: ./oisd/nsfw_small.mrs

    #  oisd_nsfw_big:
    #    <<: *rule_behavior_domain_mrs
    #    url: "https://github.com/legiz-ru/mihomo-rule-sets/raw/main/oisd/nsfw.mrs"
    #    path: ./oisd/nsfw_big.mrs

    # --------------------------------------------------------------------------
    # ВСТРОЕННЫЕ ПРАВИЛА (INLINE)
    # --------------------------------------------------------------------------

    # misc-ai — список доменов для ИИ и инструментов разработки
    # Задан напрямую, так как редко обновляется
  misc-ai:
    type: inline    # Тип: встроенный (не скачивать)
    behavior: classical    # Тип правила: классический
    format: text    # Формат: текстовый список
    payload:
        # xAI (Grok)
      - DOMAIN-SUFFIX,x.ai
      - DOMAIN-SUFFIX,grok.x.ai
      - DOMAIN-SUFFIX,api.x.ai
      - DOMAIN-KEYWORD,grok
        # Hugging Face
      - DOMAIN-SUFFIX,huggingface.co
      - DOMAIN-SUFFIX,hf.co
      - DOMAIN-SUFFIX,hf.space
      - DOMAIN-SUFFIX,huggingface.com
      - DOMAIN-KEYWORD,huggingface
        # Midjourney и другие ИИ для изображений
      - DOMAIN-SUFFIX,midjourney.com
      - DOMAIN-SUFFIX,cdn.midjourney.com
      - DOMAIN-KEYWORD,midjourney
      - DOMAIN-SUFFIX,stability.ai
      - DOMAIN-SUFFIX,stablediffusionweb.com
      - DOMAIN-SUFFIX,dreamstudio.ai
      - DOMAIN-SUFFIX,runwayml.com
      - DOMAIN-SUFFIX,runway.ml
        # Character.AI и другие
      - DOMAIN-SUFFIX,character.ai
      - DOMAIN-SUFFIX,beta.character.ai
      - DOMAIN-SUFFIX,replicate.com
      - DOMAIN-SUFFIX,cohere.ai
      - DOMAIN-SUFFIX,cohere.com
        # Инструменты для программистов
      - DOMAIN-SUFFIX,jetbrains.com
      - DOMAIN-SUFFIX,replit.com
      - DOMAIN-SUFFIX,repl.it
      - DOMAIN-SUFFIX,stackoverflow.com
      - DOMAIN-SUFFIX,docker.com
      - DOMAIN-SUFFIX,docker.io
      - DOMAIN-SUFFIX,dockerstatic.com
      - DOMAIN-SUFFIX,compose-spec.io
      - DOMAIN-SUFFIX,npmjs.com
      - DOMAIN-SUFFIX,pypi.org

# ============================================================================
# === ПРАВИЛА МАРШРУТИЗАЦИИ (RULES) ===
# ============================================================================

# Основной список правил маршрутизации
# Правила обрабатываются сверху вниз, первое совпадение применяется
rules:
    # Локальная сеть — всегда напрямую (без прокси)
    # Эти правила имеют высший приоритет для безопасности и производительности
  - IP-CIDR,192.168.0.0/16,DIRECT,no-resolve    # Частная сеть Class C
  - IP-CIDR,10.0.0.0/8,DIRECT,no-resolve    # Частная сеть Class A
  - IP-CIDR,172.16.0.0/12,DIRECT,no-resolve    # Частная сеть Class B
  - IP-CIDR,127.0.0.0/8,DIRECT,no-resolve    # Loopback (localhost)

    # Безопасность: блокировка уязвимых UDP портов
    #  - AND,((NETWORK,udp),(DST-PORT,135)),REJECT
    #  - AND,((NETWORK,udp),(DST-PORT,137)),REJECT
    #  - AND,((NETWORK,udp),(DST-PORT,138)),REJECT
    #  - AND,((NETWORK,udp),(DST-PORT,139)),REJECT
    # QUIC-трафик (UDP/443) — специальная обработка
  - AND,((NETWORK,udp),(DST-PORT,443)),QUIC

    # Реклама / NSFW
    #  - RULE-SET,oisd_small,Ad-Filter
    #  - RULE-SET,oisd_big,Ad-Filter
    #  - RULE-SET,oisd_nsfw_small,Ad-Filter
    #  - RULE-SET,oisd_nsfw_big,Ad-Filter
    # Проверка IP — через прокси для анонимности
  - OR,((DOMAIN,ipwho.is),(DOMAIN,api.ip.sb),(DOMAIN,ipapi.co),(DOMAIN,ipinfo.io),(DOMAIN,2ip.io),(DOMAIN,2ip.ru)),SELECTOR

    # Торренты — приоритетная маршрутизация
  - OR,((RULE-SET,torrent-clients),(RULE-SET,torrent-trackers)),Torrent-Traffic
  - RULE-SET,torrent-websites,Torrent-Traffic

    # ИИ и инструменты разработки — приоритетная маршрутизация
  - RULE-SET,misc-ai,AI & Разработка

    # Российский трафик (по IP-геолокации ZKEEN)
    #  - RULE-SET,ru-app-list,DIRECT
  - GEOIP,ru,Ru-Traffic

    # Мессенджеры и коммуникационные сервисы
  - OR,((RULE-SET,telegram-domains),(RULE-SET,telegram-ips)),Telegram
  - OR,((RULE-SET,whatsapp-domains),(RULE-SET,whatsapp-ips)),WhatsApp
  - OR,((RULE-SET,discord-domains),(RULE-SET,discord-voiceips)),Discord

    # Игровые платформы
  - OR,((RULE-SET,steam-domains),(RULE-SET,steam-ips)),Steam

    # ИИ-сервисы (по порядку приоритета)
  - RULE-SET,anthropic,Anthropic
  - RULE-SET,perplexity,Perplexity
  - RULE-SET,google-gemini,GoogleGemini
  - RULE-SET,openai,OpenAI
  - RULE-SET,microsoft,Microsoft

    # Стриминг и видео
  - RULE-SET,twitch-domains,Twitch
  - RULE-SET,youtube,YouTube

    # CDN и облачные сервисы (по ZKEEN GEOIP)
  - GEOIP,akamai,AKAMAI
  - GEOIP,amazon,AMAZON
  - GEOIP,fastly,FASTLY
  - GEOIP,google,GOOGLE
  - GEOIP,cloudflare,CLOUDFLARE
  - GEOIP,cdn77,CDN77

    # Правила для ускорения интернета с ECH
  - DOMAIN,cloudflare-ech.com,SELECTOR
  - RULE-SET,refilter-ech,DIRECT
  - RULE-SET,refilter-noech,ECH-Refilter
  - RULE-SET,refilter-ipsum,ECH-Refilter

    # Облачные провайдеры и хостинги (по ZKEEN GEOIP)
  - GEOIP,meta,META
  - GEOIP,digitalocean,DIGITALOCEAN
  - GEOIP,hetzner,HETZNER
  - GEOIP,mega,MEGA
  - GEOIP,oracle,ORACLE
  - GEOIP,ovh,OVH
  - GEOIP,vultr,VULTR

    # Сайты, заблокированные в РФ или блокирующие РФ (по ZKEEN geosite)
  - GEOSITE,domains,Other-Domains
  - GEOSITE,other,Other-Domains
  - GEOSITE,politic,Politic-Domains

    # Общий запасной контур для голосовых и игровых портов
  - OR,((DST-PORT,596-599),(DST-PORT,1400),(DST-PORT,3478-3480),(DST-PORT,4379-4380),(DST-PORT,5222),(DST-PORT,5223),(DST-PORT,5228),(DST-PORT,5242),(DST-PORT,6250),(DST-PORT,12000-65535),(DST-PORT,27101-65535),(DST-PORT,45395),(DST-PORT,50318),(DST-PORT,59234)),SELECTOR

    # Трафик от TUN и mixed-интерфейсов — в главный селектор
  - IN-NAME,tun-in,SELECTOR
  - IN-NAME,mixed-in,SELECTOR

    # Правило по умолчанию — весь остальной трафик
  - MATCH,SELECTOR

# ============================================================================
# КОНЕЦ КОНФИГУРАЦИИ
# ============================================================================
